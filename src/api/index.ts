// API route handlers for Next.js App Router
export { GET, createCommentHandler } from './route-handler'

// For Pages Router compatibility
export { default as pagesRouterHandler } from './pages-handler'